import { SuperHexColor } from '../types';
export declare const rd: (x: number) => number;
export declare const rd2: (n: number) => number;
export declare const flip: (o: {
    [name: string]: string;
}) => {
    [value: string]: string;
};
